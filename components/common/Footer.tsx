'use client'

import styled from 'styled-components'

const FooterWrapper = styled.footer`
  /* Placeholder — Footer component (implemented separately) */
  width: 100%;
`

const Footer = () => {
  return <FooterWrapper />
}

export default Footer
