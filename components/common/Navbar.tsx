'use client'

import styled from 'styled-components'

const Nav = styled.nav`
  /* Placeholder — Navbar component (implemented separately) */
  display: flex;
  align-items: center;
  width: 100%;
`

const Navbar = () => {
  return <Nav />
}

export default Navbar
